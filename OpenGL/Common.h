#pragma once
#include <iostream>
#include <string>
#include <fstream>
#include <sstream>
#include <vector>
#include <stdlib.h>
#include <time.h>
#include <cmath>
#include <algorithm>
#include "Constants.h"

#define min(x, y) (((x)<(y)) ? (x) : (y))
#define max(x, y) (((x)>(y)) ? (x) : (y))